<handover timestamp="2026-09-05T13:30+00:00">
## Work
Orchestrating (agents only — I do not read or write code myself; I brief, verify, commit, push) a self-directed AI for the third-party Warcraft III map Fall of Rome 1.06, living in `scripts/experimental/rome-ai/`. Fourteen playtest-driven rounds in. The current step is a pre-registered A/B experiment: a deliberately simple ~400-line procedural AI drives West Rome, Visigoths and Saxons while the ~4,500-line scoring AI drives everyone else, in one game, with matched controls — built, pushed, awaiting the owner's playtest and its `forai-events.txt`. Two things gate everything: a mid-game crash fix (boolexpr leak) has NOT been retested by the owner, and a Fable advisor review of the whole programme is in flight. A parallel translation programme (25 non-English maps → English in `test/`) is complete for now.

## Decisions
- Decision: the AI is an opponent that plays to win, not a sparring partner. Asserted by `plays_to_win()` — nothing may branch on whether an opponent is human. Reason: owner chose "the latter" explicitly.
- Decision: no material cheating; `AI_HANDICAP` 1.0, material knob present, labelled, at zero, asserted by `no_cheating()`. Difficulty is competence-only (error rate 0.35/0.15/0.05, reaction 4/2/1 s, think period 8/4/2 s). Reason: owner said "we should avoid it"; Squid Game v1.5 is the shipped precedent that honest pacing reads as strong.
- Decision: fog contract is SPLIT and disclosed in the startup banner — enemy strength is fog-honest, territorial ownership is read map-wide (`ai_ptOwner` written under the visibility guard but never read). Whether to close that in code is the OWNER'S call and is still open. Do not restate "fog is respected" flat; that claim was retracted.
- Decision: the simple-AI experiment's endpoints are pre-registered in `SIMPLE-AI-PLAN.md` and committed before the code (`d5bda33` plan → `0db459f` pre-registration → `65a3ce2` build). Primary: net territory per matched pair. Secondary: watchdog firings, churn, commitment %, time-to-first-objective. Joint reading declared: territory draw + near-zero simple-arm watchdog firings = support for the thesis AND evidence the complexity bought nothing. Do NOT revise readings after data.
- Decision: faction assignment — simple: West Rome, Visigoths, Saxons; controls: North Rome, Ostrogoths, Burgundians; unpaired references (complex, non-adjacent): Franks, East Rome; excluded: Vandals, Britons (sea-locked, ferry unimplemented), Huns (zero enemy points within 5000), Persians (stipend + 200 food, comparable to nothing). Owner asked for "several factions, one on the Roman side".
- Decision: commit incrementally after every green milestone. Reason: the container was destroyed twice mid-session; only pushed work survived.
- Decision: keep the AI narrating its REVERSALS (all four abort reasons, hero withdrawal, goal changes, objectives chosen/taken) — those diagnosed an eight-round-old bug from two chat lines. Cut housekeeping (the centroid-validity "form up" line is telemetry-only now). Recorded in DESIGN §31.1 so it is not re-litigated.
- Decision: the twelve faction voices are GENERATED from `docs/reference/fall-of-rome-voices.md` by `gen-voices.py` → `voices.j`; the trace asserts no drift. Edit the spec, not the JASS. Line variation must never draw from `AI_Rand` (it would fork the seeded decision stream).
- Decision: map balance is NOT modified by our injection; the only non-AI edits are AI notice text in the map description/name. Reason: every playtest verdict must be about the AI.
- Decision: owner rulings on decomposition findings — the map's two unimplemented victory conditions do not matter; Persia being unbribable is intentional; Persia is a peer empire, not a barbarian; the Saxon alliance research announcing "the Goths" is a MAP bug (recorded in DESIGN, never "fixed" on our side).
- Decision: `test/GodsWord_EN.w3x` (115 MB) is tracked via git-lfs, scoped to that one path in `.gitattributes`; the other 24 translations are ordinary blobs. Owner asked for a single file, not the split.
- Decision: the engine-AI path (S9) is answered YES — `.ai` scripts run on this map once slots are `MAP_CONTROL_COMPUTER` — but it is NOT integrated into the playable build (asserted zero engine-AI calls). Captain quality with a full army was never tested (probe v3 exists, never run).
- Decision: Warsmash as an outcome harness is parked. It executes this map's JASS and our AI, but needs a few MB of the owner's own game data (tables, scripts, pathing — no models/textures) which he has not supplied, and it has NO native AI VM at all. If pursued, it can test our map-script AI, never the captain path.

## State
- `scripts/experimental/rome-ai/for-ai.j` — the complex AI (~4,500 lines JASS) — shipped, HEAD `65a3ce2`.
- `scripts/experimental/rome-ai/DESIGN.md` — the full round-by-round record, §1–§31+ — the programme's memory; read it before anything.
- `scripts/experimental/rome-ai/SIMPLE-AI-PLAN.md` — experiment design + pre-registration — final.
- `scripts/experimental/rome-ai/trace.py` — harness, 645 assertions, wired into `npm test` with a PASS-count floor and per-section sweep — passing.
- `scripts/experimental/rome-ai/parse-events.py` — telemetry parser; prints territory per faction (human excluded but reported), army-exit census with commitment %, muster arrival-vs-timeout ratio, watchdog firings, mission churn, matched-pair verdict with the joint reading — final.
- `scripts/experimental/rome-ai/gen-voices.py`, `voices.j` — generated voice tables — final.
- `scripts/experimental/rome-ai/probe.py`, `probe.ai` — S9 engine-captain probe (v3, hardened) — never run in game.
- `scripts/experimental/rome-ai/inject.py`, `describe.js`, `lint_apostrophe.py`, `README.md` — build tooling — final.
- Playable build: `$SCRATCH/out/rome-ai.w3x` 19,086,080 bytes (scratch is ephemeral — rebuild via `inject.py` from source if gone). Probe: `rome-ai-S9PROBE.w3x` 19,048,128.
- `docs/reference/wc3-ai-lessons.md` — map-agnostic lessons — final.
- `docs/reference/ai-research-briefs.md` + `docs/reference/ai-research-2026-08/` (8 briefs + synthesis, external, verbatim) — final.
- `docs/reference/wc3-map-ai-decompositions.md`, `brytenwalda-ai-decomposition.md`, `squid-game-ai-decomposition.md`, `fall-of-rome-voices.md`, `warsmash-eval-2026-08.md` — final.
- `docs/reference/rome-ai-advisor-review-2026-09.md` — Fable advisor review — IN FLIGHT (agent running at handover time).
- `scripts/experimental/warsmash-eval/` — Warsmash probe kit — final, parked.
- `test/SurviveInNorthrend_EN.w3x` + README — 25th translation — committed `d344331`.
- `test/GodsWord_EN.w3x` (LFS) + README — 24th — committed `089bffc`.
- `CLAUDE.md` — last updated at the "as of 2026-08-09" block; it does NOT reflect the AI programme's rounds 3–14, the telemetry, the experiment, the 25 translations, or LFS. Needs a state update.

## Corrections
- Never compare multiboards from two different games as before/after, and always exclude the human's faction: the owner was playing Huns, and I attributed his conquests to the AI. Same-game, two times, human excluded.
- "Persia is a peer empire" — write it with confidence; it is not a barbarian by any evidence in the file.
- Naval: warfare is worthless on this map, transport is wanted; only Vandals and Britons (orange, green) should build transports; sea-locked factions consolidate their island first. The map's own hint text disagrees — the owner's verdict wins.
- Own gates: NOT "default open"; open on demand, never into a live threat, close behind a sortie. The naive rule made Romans open gates for barbarians.
- Food cap: the owner says "the food limit should go UP to 75, with extra 15 for each town they capture." Measured: ceiling 100 (200 Persia, 300 each Roman) set once at init, Roman Town tooltip Food 10, Vandals seen at 113/75. I ASKED whether that is a change request or a description; NO ANSWER yet. Do not change balance without it.
- Use the log, don't guess: the owner rejected inference when the telemetry could answer (Visigoths). Ask for `forai-events.txt` (WC3 dir or `CustomMapData`) or `-ailog` chat.
- The "four failure shapes" and "architecture is the problem" framing is MY thesis, adopted fast by the owner; the experiment exists to test it, not to confirm it.
- Every hypothesis I gave for the berserkers, the camp trap, and marching-triggers-muster was wrong; the real causes were head-of-line starvation in a fixed order slice, a 2500-unit arrival tolerance cancelling short moves, and the centroid-validity line respectively. Measure before theorising.

## Agreement Audit
- Agreed: the scoring architecture is intrinsically the source of the recurring bugs. Risk: pattern-matched by me across ~15 defects; the owner accepted it within one message; the experiment is the only test and it has not run. A competent counter-argument is that a procedural AI will simply fail in shapes we have not seen yet.
- Agreed: Brytenwalda and Squid Game "read as competent" and their steals transfer. Risk: both maps delete the hard cases (no building, gates permanently open, waygates instead of naval; one/two-bit decisions with a public answer schedule). The decompositions say this plainly; I may have over-weighted the steals.
- Agreed: the watchdog is "the fix that catches the next variant rather than the last." Risk: I specified it, it was scale-defeated on its first outing, and its army-keyed successor has never been through a full playtest.
- Agreed: many fixes are "done" on 645 green assertions. Risk: every real defect in fourteen rounds was found by the owner, zero by the harness; five instruments could not fire; a handle leak was invisible to every gate. Green means "not yet contradicted", nothing more.
- Agreed: "the voices earned their keep" (two diagnoses from chat lines). Risk: two anecdotes; the owner also found them spammy enough to ask for a mute.
- Agreed: the lessons doc's claim that nobody uses the engine AI "because the controller blocker is silent". Risk: plausible inference from usage statistics; not historically verified.
- Agreed: the crash was the boolexpr leak. Risk: found by static audit, fits "midway" perfectly, but the owner has NOT retested; if it still crashes the leak was not the whole story.

## Dead Ends
- Tried transplanting AMAI's army tracker from a second-hand description → the source has three defects (point/vector mix-up, wrong loop index, unconditional max overwrite) and is omniscient; built our own instead.
- Tried a faction-index mismatch as the cause of silent factions → refuted 12/12 from four independent map sources; the silence was ally scoping (owner plays a barbarian) plus the probe's own disable.
- Tried "Roman preplaced units not enrolled" → refuted; enrolled since round 1, never ordered (a gold floor a rich empire could not fall below).
- Tried "default open own gates" → Romans opened for barbarians.
- Tried a faction-level world-change watchdog → defeated by Roman scale (26 cities always change something); army-keyed with FIELD CV instead.
- Tried replay parsing as an outcome oracle → `.w3g` records player input only; no AI actions or outcomes.
- Tried stock W3MMD telemetry → elects emitters only from `MAP_CONTROL_USER` slots; used `PreloadGenEnd` + `-ailog` fallback with identical schema.
- Tried a 55%-of-army garrison, a 70% muster bar against all units owned, a per-dispatch order budget, a fixed order slice, a 2500 arrival tolerance → each produced a specific stuck/partial-commit behaviour; all replaced.
- Tried pushing a 115 MB file → GitHub hard limit; LFS for that one path.

## Open Threads
- ADVISOR VERDICT (landed after this handover was drafted; `docs/reference/rome-ai-advisor-review-2026-09.md`, 530 lines, commit `0484eac`) — read it FIRST, it overrides the ordering below where they conflict. Headlines: (a) not working by the programme's own criterion — the territory-over-time metric adopted in DESIGN §12.2 has never been measured for any AI faction; (b) the "four shapes" diagnosis is half story — by layer, ~6 defects were goal selection and ~19 were execution/world-model/state, and the simple AI KEEPS the execution layer; 14 defects trace to a previous round's fix (relocation, not narrowing); (c) the experiment as built cannot answer its question — matched barbarian pairs share an allied force and vision so they compete for one frontier rather than fight, Franks are not non-adjacent (6,640 from Visigoths), the Roman pair faces ~20 vs ~9 land-frontier points, the primary endpoint counts a shipyard as a capital, the simple arm's `mus` event is hard-coded "arrival 100%", the joint reading fires on 0-vs-0, the cost comparison stubs shared functions; (d) STOP adding mechanisms, stop adding source-shape guards, stop reporting the PASS count, stop treating `trace.py` as a gate; (e) DO, in order: get the one log; amend the experiment readings before interpreting it (kind-filtered per-faction deltas vs own start, geography as covariate, strike the pair-win verdict, fix the simple `mus` event) and treat the game as a pilot of the instruments; build a closed-loop toy world on the existing interpreter (kinematic units, income, supply, gates, allocation counter, outcomes over 1800 s, negative-controlled by reverting three shipped fixes) — it needs nothing from the owner and would have caught ~13 shipped defects; decide architecture from that plus the log, not the thesis; the never-run S9 captain probe v3 is the cheaper test of the execution layer; delete what the toy world proves inert, starting with the S3 projection; ship remote defence + order acknowledgement together; decide the ferry rather than defer it a seventh time; fix README.md which still says "Fog is respected".
- LEAD: the one playtest the advisor asks for — build `65a3ce2` (19,086,080 bytes), owner in the Huns seat (its only frontier is East Rome, a reference faction; nothing within 5000 of any pair member), ideally idle, otherwise not touching West/North Rome or the four barbarian pair members, to T=1800, with `forai-events.txt` and a note of whether it crashed and when. One file answers the crash retest, the first territory timeline in fourteen rounds, watchdog counts per arm, the complex arm's real muster ratio, and whether Vandals/Britons act. No second playtest until the toy world exists.
- LEAD: crash retest. The owner has not played the leak-fix build (`67d91f1` or later). If it still crashes midway, that outranks everything; bisect from the boolexpr fix.
- LEAD: the experiment playtest. Needs one full game to the end plus `forai-events.txt`. The parser prints the verdict. Do not interpret partial games.
- LEAD: read the Fable advisor review when it lands (`docs/reference/rome-ai-advisor-review-2026-09.md`) and act on its "stop doing" list before adding mechanisms.
- LEAD: update `CLAUDE.md`'s state section — it is a month stale on the AI programme.
- Ask the owner: is "75 + 15 per town" a food-cap change request? (unanswered)
- Ask the owner: fog-contract code decision — close ownership reads behind visibility, or keep map-wide and leave the banner disclosure?
- Audit backlog, in queue order, repeatedly deferred: S3 displacement is dead code (`ai_clDX/DY` always 0; needs an assertion that fires if displacement is always zero over a run); remote defence never feeds the threatened point into DEFEND; naval ferry (now the binding constraint for Vandals/Britons — real loop or keep suppressing across-water objectives); order acknowledgement (`AI_TryOrder` caches before issuing, ignores the return value).
- FOLLOW: S9 probe v3 (captains with guard positions cleared) never run; only matters if the engine path is reconsidered.
- FOLLOW: Warsmash outcome harness — only if the owner supplies the data bundle; 4.5–6 days after that, validation unbounded.
- FOLLOW: other maps' playtest queue — Last Train tower slice, vaults-of-ash never loaded in game, northreach re-verification, Name Tag Rip bisect ladder.
- Minor: Consensus MCP needs OAuth from an interactive session; never blocked anything.

## Context
- The Rome AI agent was a single long-lived background agent resumed ~40 times; its transcript lives in the ephemeral scratchpad and will not survive a restart. Relaunch fresh with DESIGN.md as the memory; every round's evidence is there.
- Scratch root: `/tmp/claude-0/-home-user-wc3/df8a77ea-156b-5f11-9fb8-e8ed8104d142/scratchpad/`. Builds go to `out/`. Third-party maps live only there and are never committed.
- Build identification: the startup line names the build; sizes are also recorded per round in DESIGN.md.
- In-game commands: `-aieasy/-ainormal/-aihard`, `-aiquiet/-aitalk`, `-aispy` (see every faction), `-aibarb`/`-airome` (mute a side), `-ailog` (mirror telemetry to chat). The `FORAI|ver|seq|t|event|…|checksum` log is always on and unaffected by mutes.
- Gates for any AI change: `pjass` FULL mode (needs `common.j`/`Blizzard.j` fetched and block-comment-stripped into a scratch `japi/`), `trace.py` 0 FAILs with the count floor, apostrophe delta 0, `validate-map` parity 191/192 + 152 warnings against the unmodified map, `npm test`, zero engine-AI calls in the playable build.
- The owner reports playtests as screenshots + one-line verdicts; his chat screenshots have diagnosed more bugs than the harness. Treat them as primary evidence and transcribe the chat lines verbatim into the brief.

## Files
- scripts/experimental/rome-ai/for-ai.j — complex AI — shipped
- scripts/experimental/rome-ai/DESIGN.md — programme record — living
- scripts/experimental/rome-ai/SIMPLE-AI-PLAN.md — experiment pre-registration — final
- scripts/experimental/rome-ai/trace.py — harness — passing
- scripts/experimental/rome-ai/parse-events.py — telemetry parser — final
- scripts/experimental/rome-ai/gen-voices.py, voices.j — generated voices — final
- scripts/experimental/rome-ai/probe.py, probe.ai — engine-captain probe — unrun
- docs/reference/wc3-ai-lessons.md — transferable lessons — final
- docs/reference/ai-research-briefs.md, ai-research-2026-08/ — external research — final
- docs/reference/wc3-map-ai-decompositions.md, brytenwalda-ai-decomposition.md, squid-game-ai-decomposition.md — decompositions — final
- docs/reference/fall-of-rome-voices.md — voice spec (source of truth for voices.j) — final
- docs/reference/warsmash-eval-2026-08.md, scripts/experimental/warsmash-eval/ — parked
- docs/reference/rome-ai-advisor-review-2026-09.md — advisor review — in flight
- test/GodsWord_EN.w3x (LFS), test/SurviveInNorthrend_EN.w3x + READMEs — translations 24 and 25 — committed
- docs/reference/handover-2026-09-05.md — this document
</handover>
